// Compat re-export — HelpTip now lives in tooltip.tsx alongside the general-purpose
// Tooltip primitive. Kept here unchanged so existing imports keep working.
export { HelpTip } from '@/components/ui/tooltip';
