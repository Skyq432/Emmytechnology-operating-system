import { getSalesTeamPerformance } from '@/lib/sales/read-server';

const money = (value: number) => `₦${Number(value || 0).toLocaleString('en-NG', { maximumFractionDigits: 0 })}`;

export default async function SalesTeamPage() {
  const team = await getSalesTeamPerformance();
  return <div className="mx-auto max-w-[1400px] space-y-5"><div><p className="text-xs font-black uppercase tracking-[0.16em] text-slate-400">Commercial quality, not just volume</p><h1 className="mt-2 text-3xl font-black text-[#032489]">Sales Team</h1><p className="mt-2 text-sm text-slate-500">Performance shows Sales Value, cash collection, outstanding exposure and Gross Profit together so heavy discounting or unpaid sales do not look artificially strong.</p></div><div className="grid gap-4 lg:grid-cols-2">{team.map((person) => { const margin = person.salesValue > 0 ? (person.grossProfit / person.salesValue) * 100 : 0; return <section key={person.userId || person.name} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><div className="text-lg font-black">{person.name}</div><div className="mt-1 text-xs text-slate-400">{person.directSales} Direct Sales · {person.orders} Orders</div></div><div className="rounded-xl bg-blue-50 px-3 py-2 text-sm font-black text-[#032489]">{margin.toFixed(1)}% GM</div></div><div className="mt-4 grid grid-cols-2 gap-3"><div className="rounded-xl bg-slate-50 p-3"><div className="text-[10px] font-black uppercase text-slate-400">Sales Value</div><div className="mt-1 font-black">{money(person.salesValue)}</div></div><div className="rounded-xl bg-slate-50 p-3"><div className="text-[10px] font-black uppercase text-slate-400">Gross Profit</div><div className="mt-1 font-black">{money(person.grossProfit)}</div></div><div className="rounded-xl bg-slate-50 p-3"><div className="text-[10px] font-black uppercase text-slate-400">Cash Collected</div><div className="mt-1 font-black">{money(person.cashCollected)}</div></div><div className="rounded-xl bg-slate-50 p-3"><div className="text-[10px] font-black uppercase text-slate-400">Outstanding</div><div className="mt-1 font-black">{money(person.outstanding)}</div></div></div></section>; })}{!team.length ? <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-sm text-slate-400">No salesperson performance data yet.</div> : null}</div></div>;
}
